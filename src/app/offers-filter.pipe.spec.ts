import { OffersFilterPipe } from './offers-filter.pipe';

describe('OffersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OffersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
