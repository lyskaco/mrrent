import { InterestOrderByPipe } from './interest-order-by.pipe';

describe('InterestOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new InterestOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
