import { DayCountdownPipe } from './day-countdown.pipe';

describe('DayCountdownPipe', () => {
  it('create an instance', () => {
    const pipe = new DayCountdownPipe();
    expect(pipe).toBeTruthy();
  });
});
